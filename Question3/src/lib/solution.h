#ifndef TEMPLATE_SOLUTION_H
#define TEMPLATE_SOLUTION_H

#include <string>
#include <vector>

class Solution {
public:
  void dupRemove(std::vector<int> &inputs);
  void dupRemoveS(std::vector<int> &inputs);
  void reversed(std::vector<int> &inputs);
  void oddRemover(std::vector<int> &inputs);
  std::vector<int> concatenating(std::vector<int> &inputs, std::vector<int> &inputs1);
  std::vector<int> uni(std::vector<int> &inputs, std::vector<int> &inputs1);
};

#endif